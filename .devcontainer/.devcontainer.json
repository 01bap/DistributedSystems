{
  "name": "SvelteKit + Gin Dev Container",
  "onCreateCommand": "echo 'Start'",
  "build": {
    "context": ".",
    "dockerfile": "Dockerfile"
  },
  
  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.shell.linux": "bash"
      }
    },
    "extensions": [
      "golang.go",
      "svelte.svelte-vscode"
    ]
  },

  "postCreateCommand": "[ -d ./frontend ] && npm install ./frontend || echo 'Kein Frontend gefunden'; [ -d ./backend ] && cd backend && go mod tidy || echo 'Kein Backend gefunden'; echo 'Post-Installationsprozess abgeschlossen'",

  "forwardPorts": [5173],
  "portsAttributes": {
    "5173": {
      "label": "Frontend (Svelte)",
      "onAutoForward": "notify"
    }
  },
  
  "remoteUser": "root"
}